spring:
  application:
    name: ingsis-test

  datasource:
    url: jdbc:h2:mem:snippet_test;DB_CLOSE_DELAY=-1
    driver-class-name: org.h2.Driver
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
    database-platform: org.hibernate.dialect.H2Dialect

  data:
    redis:
      host: localhost
      port: 0

# Azure Storage - valores fake (no se usará una conexión real)
azure:
  storage:
    connection-string: DefaultEndpointsProtocol=http;AccountName=testaccount;AccountKey=fakeKey==;BlobEndpoint=http://127.0.0.1:10000/devstoreaccount1
